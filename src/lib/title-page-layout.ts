/** Spacing/width for the main column on movie and TV title pages (below hero). */
export const titlePageSection = "mt-8 max-w-2xl" as const;

/** Stacked in-app blocks (lists / Plex). External links sit beside the poster on title pages. */
export const titlePageSectionStack = "mt-8 max-w-2xl space-y-8" as const;
